model = "gpt-5.6-sol"
model_context_window = 1000000
model_auto_compact_token_limit = 900000

[mcp_servers.mechcore]
command = "direnv"
args = ["exec", ".", "cargo", "run", "--release", "--package", "mechcore", "--", "mcp"]
enabled = true
startup_timeout_sec = 300
tool_timeout_sec = 60
