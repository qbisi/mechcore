kind: layout
round: 1

# Armour Piercing Bullets alone: one `attack_interval_rate` of `+0.3`, and
# `+2.5` of damage which lands on another number.
#
# It gives the line its offset: a rate multiplies whatever the build counts
# from, so the reading here says how far that is from the reading above.

blue:
  techs:
    sledgehammer: [armor_piercing_bullets]
  units:
    - name: sledgehammer
      index: 0
      position: {x: 5, y: -50}

red:
  units:
    - name: rhino
      index: 0
      position: {x: 5, y: -55}
