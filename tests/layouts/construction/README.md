**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**Splash on a block, in the simulator.** A shot at a block splashes the next one
— the Wraith's 8 metres reach the neighbouring block's edge exactly — and the
simulator damages only the block it aims at, without refusing. So
`wall-weapon-group.yaml` reproduces 92 of its 242 ticks and parts company at the
first splash, which is why it is measured in `line-of-fire.mcscript` and not
pinned in `regressions.mcscript`.

#**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

C**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

x**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

E**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

x**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

T**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

[**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

]**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

(**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

/**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

/**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

/**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

/**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

/**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

)**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

'**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

:**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

j**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

x**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

T**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

W**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

'**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

'**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

B**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

T**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

F**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

x**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

W**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

R**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

j**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

5**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

1**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

(**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

)**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

'**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

'**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

6**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

1**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

0**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

4**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

1**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

1**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

2**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

5**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

9**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

'**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

:**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

F**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

M**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

F**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

|**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

x**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

x**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

C**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

I**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

:**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

T**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

T**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

—**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

—**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

T**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

x**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

'**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

—**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

—**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

I**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

:**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

C**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

0**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

5**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

6**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

7**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

'**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

0**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

7**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

1**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

1**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

4**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

'**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

M**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

T**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

M**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

M**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

C**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

—**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

2**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

3**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

2**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

9**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

'**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

1**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

1**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

1**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

2**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

—**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

H**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

/**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

x**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

x**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

;**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

—**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

_**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

—**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

8**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

3**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

6**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

0**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

#**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

#**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

W**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

A**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

T**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

M**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

B**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

:**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

C**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

M**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

x**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

T**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

—**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

D**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

W**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

'**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

—**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

A**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

E**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

x**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

'**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

W**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

A**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

_**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

2**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

7**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

4**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

8**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

8**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

2**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

M**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

B**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

N**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

A**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

'**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

x**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

x**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

A**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

E**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

C**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

1**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

5**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

4**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

A**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

v**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

W**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

***What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

T**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

,**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

:**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

1**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

7**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

1**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

9**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

g**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

r**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

d**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

T**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

p**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

f**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

w**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

-**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

b**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

k**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

y**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

`**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

h**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

m**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

q**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

u**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

i**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

o**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

n**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

t**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

 **What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

s**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

c**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

a**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

l**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

e**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

.**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.


**What a unit does once the block it was shooting falls, in the simulator.** The
game idles for a tick with an empty lock and then returns to the unit behind the
wall; the simulator re-engages at once. The physics layer does not see it inside
these fights, which end within a tick or two, and the content layer that does is
not compared by any regression — `plan.md` says how it was found.

**A group's other weapon slots, in the simulator.** The simulator allocates them
among units only, so `wall-weapon-group.yaml` parts company at tick 40, when the
game points all four at the wall. It is measured and not reproduced, which is
why it is in `line-of-fire.mcscript` and not in `regressions.mcscript`.

