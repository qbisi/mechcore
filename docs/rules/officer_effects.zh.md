# 军官对战斗做什么

[English](officer_effects.md)

本索引锚定 build 2259。它说明一名军官会往它所瞄准的单位身上写下什么修正、这些修正
怎么编码、各自又够得着哪些单位。军官对**账本**做什么——折扣、收入、发的队伍——是
[`reinforce_items.md`](reinforce_items.zh.md) 和
[`config/officers.yaml`](../../config/officers.yaml) 的事。

机器可读的表是
[`config/officer_effects.yaml`](../../config/officer_effects.yaml)，由
`scripts/extract-officer-effects.py` 从 build 自己的 `ConfigDataContainer` 写出。
build 的 132 名军官里，79 名带有修正。

**这些数字拿游戏自己的话核对过。** [`officers.md`](officers.zh.md) 用本地化文案列出每名
军官的效果，那是从 build 2227 用完全另一条路读出来的。79 名里有 73 名在那边写了百分比，
而每一个百分比都在这张表里：空军专家的"攻击 +13%、生命 +13%"就是 `damage_rate` 和
`life_rate` 的 `+0.13`，先进进攻战术的 30% 就是 `+0.3`，先进飞弹装置的 200% 就是
`land_mine_rate` 的 `+2`。提取脚本在发现不一致时**拒绝写表**，所以一次误解析必须同时和
一个更早 build 的独立读法对上才能蒙混过关。

## 它落在哪里

军官是一个修饰符，不是一条自己的规则。`Officer.AddData` 把军官交给
`MechDataModifer.TryAddCommonData`，再到 `MechDataModifer.AddData` 和
`SkillDataModifier.AddData`——正是
[`architecture.md`](../spec/simulation/architecture.zh.md) 说的那两层覆盖，一层在单位
上、一层在它的技能上。军官本身就是给每条条目打标签的那个 `IDataModifier`，所以把军官
拿走，它写的修正也跟着走。

这些字段就是 `GameRiver.OfficerData` 用来实现
`ICommonMechDataChangeDataSource` 的那一组。`TechnologyData`、`EquipmentData` 和
`EnergyTowerSkillData` 实现的是同一个接口，所以一种表的形状服务四个来源；另外三个等它们
各自的对象被解析出来就并进这张表。

## 一个数怎么读

| 种类 | 编码 | 例 |
| --- | --- | --- |
| `*_rate` | `FPoint`，Q32.32 原始整数 | `1288490188` 是 `+0.3` |
| `*_value` | `FPoint`，单位是那个数自己的单位 | `42949672960` 是射程 `+10` |
| `speed_value`、`extra_life`、`exp_rate` | 普通整数，由运行时 getter 的返回类型决定 | `3` |

文件里存的是**原始整数**，因为那才是 build 存的、也是定点模拟需要的。每行后面的注释是同
一个数读成小数的样子，之所以只是注释，是因为小数才是有损的那个：`1288490188 / 2^32` 是
`0.29999999981`，而 build 从不拿 `0.3` 去算。

## 一条修正怎么合成

```text
(base + Σ value) × (1 + Σ enhance) × Π (1 − impair)
```

最后向零截断一次。这个形状是 build 自己的，
[`architecture.md`](../spec/simulation/architecture.zh.md) 从 `DataSet` 的两个聚合类里读
出了它；下面是每一条子句拿游戏量出来的结果。

**削弱不是负的增强。** 增强在同一个括号里相加，削弱各自贡献一个因子：两条 `0.11` 留下的是
`0.89 × 0.89 = 0.7921`，不是 `1 − 0.22 = 0.78`。

### 增强相加

比率对描述**乘一次**，同一个数上的两条增强**先相加再乘**。

`tests/layouts/modifier/composition.mcscript` 对着游戏把它测了出来。一个长弓打一个犀牛，打两
下，三份录制里犀牛都活到最后，所以读数就是它在 19297 里还剩多少：

| 蓝方军官 | 犀牛剩余生命 | 每击伤害 |
| --- | ---: | ---: |
| 无 | 14639 | 2329，描述原样 |
| 先进进攻战术 | 13243 | 3027 |
| 先进进攻战术 ×2 | 11845 | 3726 |

3027 是 `trunc(2329 × 1.3)`，3726 是 `trunc(2329 × 1.6)`。若两条比率连乘，就是
`trunc(2329 × 1.69)` = 3935、剩 11427，而游戏打出来的不是这个数。截断在第一个数上就已经
看得见：`2329 × 1.2999999998` 是 3027.6999，build 留下的是 3027。

录像还从问题的**另一头**把同一件事说了一遍，那一头由 `mechcore fight stats` 读出。
`once` 那份在长弓的**技能**通道里存着
`damage_rate.add = 1288490188`，正是军官表里的 `+0.3`；`twice` 那份存的是
`2576980376`，恰好是它的两倍。**build 在写入的时候就把两名军官加好了**，所以录像里是一
条 `+0.6`，不是两条 `+0.3`——模拟器把条目求和，是在照抄 build 存的样子，而不是自己发明
一个条目间的顺序。两份录像里单位覆盖层和 buff 聚合都是中性的：这名军官的伤害根本不落在
单位上。

无军官那份先录了两遍、逐 tick 比对相同，所以上面的差异是军官造成的，不是管线的。

### 削弱相乘

`tests/layouts/modifier/impairment.mcscript` 对另一个符号问了同一个问题，答案不一样。成本控制
专家对所有单位的伤害和生命都是 `−0.11`，而且一方可以持有两份：

| 红方军官 | 犀牛满血 | 挨两下后剩 |
| --- | ---: | ---: |
| 成本控制专家 | 17174 | 12516 |
| ×2 | **15285** | **10627** |

`19297 × 0.89 × 0.89` 是 15285。`19297 × (1 − 0.22)` 是 15051，而游戏打的不是那一场。
一条削弱分不出两种规则——两边都是 `0.89`——所以那份录像正是这次实验的对照。

**存的那一半说得比打出来的更利落**，而且根本不用打：两份军官那份录像里 `life_rate` 的
`reduce` 是 **892923700**，正是 `1 − 0.89²` 一位不差。如果削弱相加，会存成 944892804。

两份录像的哈希和四个数都是模拟器先算出来、写进脚本、然后才开游戏的，而这个预测来自
`MultiplicativeDataFloat.Refresh`，不是猜的。

### 一个 value 按本单位相加

`tests/layouts/modifier/value.mcscript` 关掉了最后一条子句。增程弧光是射程 `+20`、伤害 `−0.2`，
所以一份录像里同时带着一个 value 和一条削弱。弧光的射程是 95 米，带上军官后提前二十米开火，
之后每一 tick 都跟着变。

游戏录出来的和模拟器的预测逐 tick 相同——同样 137 tick、同样的物理哈希、犀牛剩 16961 对
照组 16377——用的规则就是在描述所用的量化米上 `range + 20_000`。录像里技能通道存着
`attack_range_value = 85899345920`，正是表里那个原始 `+20` 原样不动，旁边那条 `damage_rate`
的 `reduce` 半边装着那次削弱。

所以一个 value 就是按本单位相加，也就是 `AdditiveDataFloat` 除了求和什么都不做的意思。

**这个求和跨来源也成立。** `technology.mcscript` 让同一只弧光既研发射程强化（科技 `+40`）
又带着增程弧光（军官 `+20`）：游戏给出的是 95 的描述变成 **155 米**，而且把两者存成**一条**
`attack_range_value = +60`，和它把两名军官的比率存成一条完全一样。这也正是这张表的规则同时
就是 [`technology_effects.md`](technology_effects.zh.md) 的规则的原因。

### value 先于 rate

公式里的那个顺序，在录像开始携带攻击间隔之前，一直只是 build 的类结构而不是一次测量。
`tests/layouts/modifier/interval-order.mcscript` 在铁锤身上把它量掉了——铁锤是唯一能在同一
个数上同时持有两种修正的单位：机械狂暴是 `-1` 秒的 value，穿甲弹是 `+0.3` 的 rate。

| 铁锤持有 | 间隔读数 |
| --- | ---: |
| 无 | 83 |
| 机械狂暴 | 63 |
| 穿甲弹 | 110 |
| 两者 | **84** |

前三份把这条直线钉死——一秒是二十个 tick，而 build 对这个单位是从比描述低七个 tick 的地方
开始数的——在那条线下，两种顺序给出不同的整数：

```text
value 先   ((83 + 7) − 20) × 1.3 − 7 = 84
rate 先    ((83 + 7) × 1.3 − 20) − 7 = 90
```

游戏打出来的是 84。**value 先加进描述，rate 再乘上去**，正是公式所写、而此前没有任何东西
证明过的那一条。

注意这次测量**不需要**知道 build 的整数从哪儿开始数：三份 fixture 无论零点在哪都能把直线
钉死，所以那个未解的偏移对实验毫无代价——`mcfr.md` 把那个偏移作为它自己的未决项记着。

### 普通整数也是 value，而且相加

`speed_value` 存成普通 `int` 而不是 `FPoint`，这是 build 在说它住在第三张列表
`DataSet.intDatas` 里。那张列表的算术取决于条目是哪个 `DataInt` 子类建的：
`DataIntGroup` 求和再钳制，`DataIntSingleMax` 只留最大的一条，`DataIntSingleMin` 留最小
的。`FightMech` 的构造函数建的是 `DataIntGroup(0x80000000, 0x7FFFFFFF, 0)`——单位的整数
求和，钳制范围是整个 `Int32`，永远不生效。

`tests/layouts/modifier/speed.mcscript` 把这条拿去问了游戏。先进动力系统和速度专家各是
全体 `+3` 移速，都放在那只要走 105 米才够得着长弓的犀牛身上——那段路就是这场仗的全部时钟：

| 红方军官 | 犀牛速度 | 战斗结束于 |
| --- | ---: | ---: |
| 无 | 16 | 第 120 tick |
| 一份 | 19 | 第 104 tick |
| 两份 | **22** | **第 92 tick** |

如果是 `SingleMax`，两条 3 的答案还是 3，第二份录像就会是第一份的翻版。录像里存的
`move_speed_value` 是 **6**。三场的剩余生命都是 14639，因为长弓怎么都只打得到两下：
这次量的是时钟，不是伤害。

它同时定死了单位：`+3` 就是每秒三米，和游戏自己文案里的数一样，不是任何东西的比例。

### 直接读一条子句

上面每一次捕获都被设计成让**战斗结果**能分开候选假设：伤害靠两次命中的生命差、射程靠
第一枪提前、移速靠 tick 数。从 MCFR 0.4.0 起录像同时带着每个单位的派生值，所以
`mechcore fight stats <recording>` 一个 tick 就给出两半——写进去的那条 `+0.6`，和 build
由它算出来的 3726。上面所有读数都用这种方式重读过一遍，全部一致。

下一条子句不再需要那样设计：把修正放到一个单位上，录一个 tick，读那个数。

## 一个字段落在哪条通道

录像给每个字段只留了**一个**位置，所以修正住在哪条通道是录像自己的形状，不是这里做的
选择：MCFR 的技能修饰符集合里有 `damage_rate`、`attack_range_rate`、
`attack_interval_rate`，单位修饰符集合里有 `life_rate` 和速度那几项。

两条通道都测过了。上面那次伤害捕获在长弓的技能通道里读到 `+0.3`。第二次捕获
`tests/layouts/modifier/officer-life-rate.yaml` 把先进防御战术的 `life_rate` 放在犀牛身上：游戏
把它存进**单位**通道，犀牛的 19297 变成 25086，挨两下之后剩 20428——这三个数都是模拟器
在录制存在之前先算出来的，整场战斗的哈希也一模一样。

## 这个 build 应用哪些军官

`crates/simulation/src/officers.rs` 把表里的一行变成落在目标单位上的修正，打上 `Modifier`
标签，所以拿走军官就拿走它写的东西。79 行里应用了 **61** 行：**每个字段都是比率、value
或普通整数，且落在模拟器推导得出的那几个数上**（伤害、生命、攻击间隔、攻击距离、移动
速度），并且 `mech_type` 是 0、1 或 10。

其余的会指名拒绝持有它的那一方：

| 为什么 | 行数 | 怎么才能关掉 |
| --- | ---: | --- |
| 修的是塔、护盾、地雷、部署时钟、经验或弹体寿命 | 11 | 拥有那个对象的机制 |
| `*_by_kill_count` | 3 | 一个数击杀数的机制 |
| `splash_range_value` | 3 | 模拟器推导的数里还没有溅射半径 |
| `mech_type` 4 | 1 | 知道哪些单位算"远程" |

**剩下的已经不再是"修正怎么合成"的问题了。** `DataSet` 三张列表全部读过、也全部量过；
每一条还在拒绝的，要么是这个模拟器没有的机制，要么是没人枚举过的目标类别。

一名军官不会被"应用一半"：这个 build 合成不出来的，就拒绝持有它的那一方——一场带着三分
之二个军官的战斗，它的数没有人能核对。

## 一条修正够得着哪些单位

每一行都带着 build 存的 `mech_type`，以及——在 build 解析出来的情况下——与之配套的单位
清单：

| `mech_type` | 行数 | 是否列出单位 | 读作 |
| ---: | ---: | --- | --- |
| 10 | 61 | 列出 | 就是这些单位 |
| 0 | 10 | 不列 | 所有单位 |
| 11 | 6 | 不列 | 不针对单位：修正落在塔、护盾或地雷上 |
| 1 | 1 | 列出七个空军单位 | 就是这些单位 |
| 4 | 1 | 不列 | 远程单位 |

类型 10 是常见情形，不需要解读：改进型铁锤列着铁锤，修正的就是铁锤。类型 0 不带清单、
够得着一切，先进进攻战术的 `+0.3` 伤害就是这一类——那正是一方可以持有两份的军官，
`docs/spec/document/layout.md` 把 `officers` 定成多重集就是为了它。类型 11 的行修正的是
`tower_life_rate`、`energy_shield_rate`、`land_mine_rate` 或 `extra_life`，没有一个是单位
的数值。

类型 4 只有一行，先进瞄准系统，射程 `+10`，不带单位清单。build 的数据里没有说明这个类别
选中什么；军官索引的文案说了——"所有**远程**单位射程 +10"——所以这个类别是从效果文案读出来
的，不是从数据读出来的。至于哪些单位算远程，两边都没说。

## 那四个特别的字段修的是什么

有四个字段修正的不是单位的数值，军官索引的文案说明了每一个是什么：

| 字段 | 修正什么 |
| --- | --- |
| `tower_life_rate` | 能量塔的生命；能量塔过载的 `+1` 就是它的"100%" |
| `energy_shield_rate` | 能量护盾装置的护盾；先进护盾装置的 `+0.4` |
| `land_mine_rate` | 哨戒飞弹装置的伤害；先进飞弹装置的 `+2` |
| `super_deployment_time_rate` | 后方部署的传送时间；快速传送的 `-0.5` |

`exp_rate` 是同类里的第五个，也是唯一一个**根本不是比率**的：它的运行时 getter 返回
`int`，五行取值是 `100` 或 `75`，文案写的是"经验成长率 +100%"，所以那个整数就是百分比。

`extra_life` 存得像个单位数值，其实不是。它唯一那一行是紧急避险，文案描述的是一方的生命
被重置为 1，而不是哪个单位多了命——所以这个字段的名字并不是它做的事。

## 本索引未确立的

- **`*_value` 修正怎么合成**——表里 79 行有 44 行在等它——以及同一个数被两条通道修正时
  谁先谁后。上面两次捕获分别在两条通道里定下了比率，这两条都没够着。
- **哪些单位算"远程"**，即 `mech_type` 4 选中谁，数据和文案都没有列举。
- **上面那四个字段落地之后做什么**：这里还没有任何机制读塔的生命、地雷、护盾装置或部署
  时钟。
